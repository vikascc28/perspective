[package]
name = "perspective"
version = "0.1.0"
edition = "2018"

build = "build.rs"


[lib]
crate-type = ["cdylib", "rlib"]
path = "src/rust/msum.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
